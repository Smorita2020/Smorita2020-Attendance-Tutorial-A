<% provide(:title, 'Base Index') %>
<% provide(:class_text, 'label-signup') %>
<% provide(:button_text, '拠点情報追加') %>
<h1>拠点一覧</h1>
  <div class="col-12">
    <table class="table table-striped table-bordered">
      <thead>
        <tr>
          <th></th>
          <th></th>
          <th class="text-center">拠点番号</th>
          <th class="text-center">拠点名</th>
          <th class="text-center">勤怠種類</th>
        </tr>
      </thead>
      
      <tbody>
        <% @base.each do |b| %>
        <tr>
          <td><%= link_to "修正", edit_basis_path(b), class: "btn btn-md btn-primary" %></td>
          <td class="text-center"><%= link_to "削除", basis_path(b), method: :delete, class: "btn btn-md btn-primary",
              data: { confirm:"削除してよろしいですか？" } %></td>
          <td class="text-center"><%= b.base_id %></td>
          <td class="text-center"><%= b.base_name %></td>
          <td class="text-center"><%= b.base_type %></td>
        </tr>
        <% end %>
      </tbody>
      
    </table>
    <%= link_to "拠点情報追加", new_basis_path, class: "btn btn-lg btn-primary" %>
  </div>
